pipeline {
    agent any  // Runs on any available agent

    stages {
        stage('Checkout') {
          
        }

        stage('Build') {
 
        }

        stage('Test') {

        }

        stage('Deploy') {

        }
    }
}
